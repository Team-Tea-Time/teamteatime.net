import { MomentAgoDirective } from './moment-ago.directive';

describe('MomentAgoDirective', () => {
  it('should create an instance', () => {
    const directive = new MomentAgoDirective();
    expect(directive).toBeTruthy();
  });
});
